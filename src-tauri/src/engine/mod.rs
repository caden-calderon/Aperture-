//! Context engine — manages universal blocks, zones, and compression.
//!
//! This module provides the core data model that mirrors the frontend
//! TypeScript types, enabling the backend to understand and manipulate
//! context blocks independently of the UI.

pub mod block;
pub mod types;
